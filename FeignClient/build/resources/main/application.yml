server:
  port: 8089
spring:
  application:
    name: FEIGN-CLIENT
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
feign:
  client:
    name: FEIGN-SERVICE
