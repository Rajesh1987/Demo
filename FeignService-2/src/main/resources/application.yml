server:
  port: 8091
spring:
  application:
    name: feign-service
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}

